[package]
name = "jitvm"
version = "0.1.0"
edition = "2021"

[dependencies]

[target.'cfg(target_arch = "x86_64")'.dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
